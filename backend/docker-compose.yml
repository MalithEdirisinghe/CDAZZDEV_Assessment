version: '3.8'

services:
  db:
    image: postgres:15-alpine
    container_name: teamsync_db
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: teamsync
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d teamsync"]
      interval: 5s
      timeout: 5s
      retries: 5

  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: teamsync_api
    restart: always
    ports:
      - "3000:3000"
    environment:
      PORT: 3000
      DATABASE_URL: "postgresql://postgres:postgres@db:5432/teamsync?schema=public"
      JWT_SECRET: "dev-jwt-access-secret-12345"
      JWT_REFRESH_SECRET: "dev-jwt-refresh-secret-12345"
    depends_on:
      db:
        condition: service_healthy

volumes:
  postgres_data:
